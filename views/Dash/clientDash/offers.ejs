<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <link
  rel="icon"
  href="/HomeImages/iconimage-fotor-20250127122413.png"
  type="image/png"
/>
    <title>Client Offers</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/pop.css" />
    <style>
      /* Responsive Card Grid */
      .offers-container {
        display: grid;
        grid-template-columns: repeat(
          auto-fill,
          minmax(320px, 1fr)
        ); /* Slightly increased card width */
        gap: 30px;
        padding: 20px;
        box-sizing: border-box;
        margin-top: 30px;
      }

      /* Individual Card Style */
      .offer-card {
        background-color: #fff;
        border: 1px solid #ddd;
        border-radius: 12px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        height: 100%;
      }

      .offer-card:hover {
        transform: translateY(-10px);
        box-shadow: 0 16px 32px rgba(0, 0, 0, 0.2);
      }

      /* Card Content Area */
      .offer-card-header {
        background-color: #f5f5f5;
        padding: 16px;
        border-bottom: 1px solid #ddd;
      }

      .offer-card-title {
        font-size: 1.4rem;
        font-weight: bold;
        margin: 0;
        display: flex;
        flex-direction: row;
        justify-content: space-between;
      }

      .offer-card-meta {
        font-size: 1rem;
        color: #888;
      }

      /* Card Body */
      .offer-card-body {
        padding: 20px;
        flex: 1;
      }

      .offer-card-description {
        font-size: 1rem;
        color: #555;
        margin-top: 10px;
      }

      .offer-card-budget {
        font-size: 1.2rem;
        font-weight: bold;
        margin-top: 15px;
      }

      .offer-card-deadline {
        font-size: 1rem;
        color: #888;
        margin-top: 10px;
      }

      /* Bids Section */
      .bids-list {
        margin-top: 20px;
        max-height: 300px; /* Limiting height */
        overflow-y: auto; /* Scrollable if there are many bids */
      }

      .bid-item {
        border-bottom: 1px solid #ddd;
        padding: 15px 0;
      }

      .bid-item:last-child {
        border-bottom: none;
      }

      .bid-item p {
        margin: 5px 0;
      }

      /* Buttons for Bid Actions */
      .action-buttons {
        display: flex;
        justify-content: space-between;
        margin-top: 10px;
      }

      .action-btn {
        padding: 10px 20px;
        border: none;
        font-weight: bold;
        cursor: pointer;
        border-radius: 5px;
        transition: background-color 0.3s ease;
        width: 48%; /* Ensure the buttons stay apart */
      }

      .accept-btn {
        background-color: #28a745;
        color: white;
      }

      .accept-btn:hover {
        background-color: #218838;
      }

      .reject-btn {
        background-color: #dc3545;
        color: white;
      }

      .reject-btn:hover {
        background-color: #c82333;
      }

      /* Show More Button */
      .show-more-btn {
        background-color: #007bff;
        color: white;
        border: none;
        padding: 10px;
        width: 100%;
        border-radius: 5px;
        cursor: pointer;
        font-weight: bold;
        transition: background-color 0.3s ease;
      }

      .show-more-btn:hover {
        background-color: #0056b3;
      }

      /* Responsive Adjustments */
      @media (max-width: 768px) {
        .offers-container {
          grid-template-columns: repeat(
            auto-fill,
            minmax(280px, 1fr)
          ); /* Slightly smaller width for medium screens */
        }

        .action-buttons {
          flex-direction: column; /* Stack buttons vertically on smaller screens */
          gap: 10px;
        }

        .action-btn {
          width: 100%; /* Buttons take full width on small screens */
        }
      }

      @media (max-width: 480px) {
        .offers-container {
          grid-template-columns: 1fr; /* Single column on small screens */
        }
      }
    </style>
    <style>
      /* General Green Dot Style */
      .green-dot {
        width: 20px; /* Set the width of the dot */
        height: 20px; /* Set the height of the dot */
        border-radius: 50%; /* Make the dot circular */
        background-color: #28a745; /* Green color */
        display: inline-block; /* To align the dots next to each other */
        margin-right: 10px; /* Space between dots */
      }
      #green-dot-2 {
        background-color: #4caf50; /* Slightly different shade of green */
      }
    </style>

    <style>
      /* Styling for College Image in Circular Shape */
      .college-image-container {
        display: flex;
        justify-content: flex-start;
        align-items: center;
        margin-bottom: 10px;
      }

      .college-image {
        width: 40px; /* Set the size of the image */
        height: 40px; /* Set the size of the image */
        border-radius: 50%; /* Makes the image circular */
        object-fit: cover; /* Ensures the image fills the circle without distortion */
        border: 2px solid #ddd; /* Optional border around the image */
        margin-right: 12px; /* Adds some space between the image and text */
      }
    </style>
    <style>
      /* Container for the filter buttons */
      .filter-buttons {
        display: flex;
        justify-content: center;
        gap: 20px; /* Space between buttons */
        margin-bottom: 20px;
        padding: 10px;
        flex-wrap: wrap; /* Allow wrapping on smaller screens */
      }

      /* Individual filter button style */
      .filter-button {
        background-color: #f4f4f4; /* Light gray background */
        color: #333; /* Dark text */
        border: 1px solid #ddd; /* Border with subtle color */
        padding: 12px 24px; /* Padding for better click area */
        font-size: 1rem;
        text-align: center;
        border-radius: 5px; /* Rounded corners */
        cursor: pointer;
        transition: all 0.3s ease; /* Smooth transition on hover */
        min-width: 150px; /* Ensure a minimum width for the buttons */
        text-decoration: none; /* Remove default link styles */
        display: inline-block; /* Ensure buttons are inline */
      }

      /* Hover effect */
      .filter-button:hover {
        background-color: #e0e0e0; /* Slightly darker gray on hover */
        border-color: #bbb; /* Change border color on hover */
      }

      /* Active state when button is clicked */
      .filter-button:active {
        transform: scale(0.98); /* Slightly shrink the button on click */
      }

      /* Responsive Design: Stack buttons on smaller screens */
      @media (max-width: 768px) {
        .filter-buttons {
          justify-content: center; /* Align the buttons to the center */
        }

        .filter-button {
          width: 100%; /* Make buttons take full width on smaller screens */
          max-width: 300px; /* Limit the max width on very small screens */
          margin-bottom: 10px; /* Add space between buttons */
        }
      }

      @media (max-width: 480px) {
        .filter-button {
          width: 100%; /* Full width on mobile screens */
          padding: 12px 20px; /* Adjust padding for mobile */
        }
      }
    </style>
  </head>

  <body>
    <!-- Include Navigation -->
    <%- include('../partials/nav') %>

    <main>
      <div class="container123">
        <h3>Welcome, Client</h3>
        <p>
          Explore the available offers and manage your projects with ease. You
          can accept or reject bids from workers.
        </p>
        <!-- Filter Buttons -->
        <div class="filter-buttons">
          <a href="/client/Offer" class="filter-button" id="all-projects-btn"
            >All workers</a
          >
          <a
            href="/client/FilteredOffer"
            class="filter-button"
            id="own-college-projects-btn"
            >your College Workers</a
          >
        </div>
      </div>

      <div class="offers-container">
        <% if (offers.length > 0) { %> <% offers.forEach(function({ project,
        bids , pendingBidsCount}) { %>
        <div class="offer-card" id="project-<%= project._id %>">
          <!-- Offer Card Header -->
          <div class="offer-card-header">
            <h4 class="offer-card-title">
              <%= project.assignment_title %> <% if (pendingBidsCount>0) { %>

              <div id="green-dot-2" class="green-dot"></div>
              <% } %>
            </h4>
          </div>

          <!-- Offer Card Body -->
          <div class="offer-card-body">
            <p class="offer-card-description">
              <strong>Description:</strong> <%= project.description %>
            </p>
            <p class="offer-card-budget">
              <strong>Budget(INR):</strong> <%= project.budget %>
            </p>
            <p class="offer-card-deadline">
              <strong>Deadline:</strong> <%= new
              Date(project.deadline).toLocaleDateString() %>
            </p>

            <!-- Bids Section -->
            <div class="bids-list">
              <h3 style="color: black">Bids:</h3>
              <% if (bids.length > 0) { %> <%
              bids.slice(0,bids.length).forEach(function(bid) { %>
              <div class="bid-item" id="bid-<%= bid._id %>">
                <!-- new new new new new new  -->
                <p><strong>Worker:</strong> <%= bid.worker.userName %></p>
                <!-- Inside the custom-card-content div -->
                <div class="college-image-container">
                  <img
                    src="<%= bid.worker.profile.collegeImage %>"
                    alt="<%= bid.worker.profile.collegeImage %> College"
                    class="college-image"
                  />
                </div>
                <!-- new new new new new new  -->
                <p><strong>Amount:</strong> <%= bid.amount %></p>
                <p>
                  <strong>Description:</strong> <%= bid.description ||
                  'Nodescription provided' %>
                </p>
                <p>
                  <strong>Deadline:</strong> <%= new
                  Date(bid.deadline).toLocaleDateString() %>
                </p>

                <% if (bid.status === 'pending') { %>
                <div class="action-buttons">
                  <button
                    class="action-btn accept-btn"
                    data-project-id="<%= project._id %>"
                    data-worker-id="<%= bid.worker.id %>"
                    data-bid-id="<%= bid._id %>"
                  >
                    Accept Bid
                  </button>
                  <button
                    class="action-btn reject-btn"
                    data-project-id="<%= project._id %>"
                    data-worker-id="<%= bid.worker.id %>"
                    data-bid-id="<%= bid._id %>"
                  >
                    Reject Bid
                  </button>
                </div>
                <% } else { %>
                <p><strong>Status:</strong> <%= bid.status %></p>
                <% if (bid.status=="accepted") { %>
                <a
                  href="/client/workerProfile/<%=bid.worker._id  %>"
                  style="text-decoration: none"
                  >Look Worker Profile</a
                >
                <% } %> <% } %>
              </div>
              <% }) %>

              <!-- Show More Button if there are more than 3 bids
              <% if (bids.length > 3) { %>
              <button class="show-more-btn" id="show-more-<%= project._id %>">
                Show More
              </button>
              <% } %> <% } else { %>
              <p>No bids available yet for this project.</p>
              <% } %> -->
            </div>
          </div>
        </div>
        <% }) %> <% } else { %>
        <p>No projects with bids available at the moment.</p>
        <% } %>
      </div>
    </main>

    <!-- Include the nav.ejs partial -->
    <%- include('../partials/pop') %>
    <!-- Scripts -->
    <script src="/scripts/app.js"></script>
    <script src="/scripts/main.js"></script>
    <script src="/scripts/pop.js"></script>
    <script>
      // Show More Bids Functionality
      document.querySelectorAll(".show-more-btn").forEach((button) => {
        button.addEventListener("click", function () {
          const projectID = button.id.split("-")[2]; // Extract the project ID from the button ID
          const bidList = document.querySelector(
            `#project-${projectID} .bids-list`
          );
          const hiddenBids = bidList.querySelectorAll(
            ".bid-item:nth-child(n+4)"
          );
          hiddenBids.forEach((bid) => (bid.style.display = "block")); // Show the hidden bids
          button.style.display = "none"; // Hide the "Show More" button
        });
      });

      function updateBidStatus(bidID, status, projectID, workerID) {
        fetch(`/client/bidStatus/${bidID}/${projectID}`, {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
          },
          body: JSON.stringify({ status }), // Send the status along with bidID and projectID
        })
          .then((response) => response.json())
          .then((data) => {
            if (data.success) {
              const bidElement = document.getElementById(`bid-${bidID}`);
              bidElement.querySelector(".accept-btn")?.remove();
              bidElement.querySelector(".reject-btn")?.remove();
              const statusParagraph = document.createElement("p");
              const aTag = document.createElement("a");
              statusParagraph.innerHTML = `<strong>Status:</strong> ${status}`;
              bidElement.appendChild(statusParagraph);
              if (status == "accepted") {
                aTag.innerHTML = "Look Worker Profile";
                aTag.setAttribute("href", `/client/workerProfile/${workerID}`);
                aTag.style.textDecoration = "none";
                bidElement.appendChild(aTag);
              }

              showMessage(
                `Offer ${status.charAt(0).toUpperCase() + status.slice(1)}!`
              );
            } else {
              showError("Error updating bid status");
            }
          })
          .catch((error) => {
            console.error("Error:", error);
            alert("An error occurred while updating the bid status.");
          });
      }

      document.querySelectorAll(".accept-btn").forEach((button) => {
        button.addEventListener("click", function () {
          const bidID = button.dataset.bidId;
          const workerID = button.dataset.workerId;
          const projectID = button.dataset.projectId; // Make sure you're passing projectId here
          updateBidStatus(bidID, "accepted", projectID, workerID);
        });
      });

      document.querySelectorAll(".reject-btn").forEach((button) => {
        button.addEventListener("click", function () {
          const bidID = button.dataset.bidId;
          const workerID = button.dataset.workerId;
          const projectID = button.dataset.projectId; // Make sure you're passing projectId here
          updateBidStatus(bidID, "rejected", projectID, workerID);
        });
      });
    </script>
    <script type="module" src="/scripts/foreground.js"></script>
  </body>
</html>
